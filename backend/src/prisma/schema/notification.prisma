model Notification {
  id      Int              @id @default(autoincrement())
  type    NotificationType
  title   String
  message String

  @@map("notifications")
}

enum NotificationType {
  reminder
}
