# Environment variables template for Backend on Railway
# Copy these to your Railway Backend service's Environment Variables

# Database - Use Railway's PostgreSQL reference
DATABASE_URL=${{Postgres.DATABASE_URL}}

# JWT Configuration
JWT_SECRET=change-this-to-a-secure-random-string-min-32-characters
JWT_EXPIRES_IN=7d

# CORS - Use Railway's Frontend domain reference
CORS_ORIGIN=https://${{Frontend.RAILWAY_PUBLIC_DOMAIN}}

# Node Environment
NODE_ENV=production
PORT=5000
