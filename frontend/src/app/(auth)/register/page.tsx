import RegisterForm from '@/features/auth/components/RegisterForm';

export default function RegisterPage() {
    return (
        <div className="min-h-screen bg-gradient-to-br from-brand-50 via-jlt-cream to-jlt-sage flex items-center justify-center p-4">
            <RegisterForm />
        </div>
    );
}